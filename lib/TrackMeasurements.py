

def TrackMeasurement():

	def __init__(self, 
		         name,
		         function,
		         axisLabel,
		         axisLimits,
		         colorMap = "",
		         description = ""):

		if (name not type.String or
			axisLabel not type.String or 
			axisLabel not type.String) or
         function,
         axisLabel,
         axisLimits,
         colorMap = "",
         description = ""):
			

		self.name = name
		self.function = function
		self.axisLabel = axisLabel
		self.axisLimits = axesLimits
		self.colorMap = colorMap



	TrackMeasurement("avgMov")